- apunte para README conector MongoDB
    --> errors.tolerance = all, nos importa más la continuidad que la consistencia en alertas IoT

- apunte para README App SalesSummary
    --> sales-transactions se consume con GenericAvroSerde
    --> el avro no es el mismo que el del DataGen, es autogenerado por el conector JDBC
    --> como no controlamos el esquema del productor consumimos con genérico por flexibilidad
    --> en contexto Enterprise se podría escribir a un sales-transactions-raw y tener una app KafkaStreams intermedia para normalizar y escribir con un esquema avro específico
    --> se consumiría luego con este esquema, para mantener la simplicidad y seguir el modelo propuesto en enunciado se mantiene directo

- apunte para README App SalesSummary
    --> tipo de price es org.apache.kafka.connect.data.Decimal (type byte, logicalType decimal)
    --> esquema verificado con curl -s http://localhost:8081/subjects/sales-transactions-value/versions/latest | jq -r '.schema'
    --> necesario convertir el campo price a float

- apunte para README DataGen sensor-telemetry
    --> se añade timestamp mediante SMT para comportamiento realista
    --> opción simple no funciona (el metadato timestamp que usa esta SMT es null en estos records sintéticos)
        transforms=AddTs
        transforms.AddTs.type=org.apache.kafka.connect.transforms.InsertField$Value
        transforms.AddTs.timestamp.field=event_ts
    --> solución: se emplea un plugin con SMT de que hace timestamp.now() en UTC
    --> se añade la descarga del plugin al setup.sh